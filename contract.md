# Contract — механика экрана (ring_mechanics_v2.html)

## Принцип движения
- Один глобальный `scroll`; каждый слой = `scroll × константа`.
- Скорости постоянные, линейные. Никаких ease/задержек старта/кусков скорости между зонами.
- Базовый отсчёт: `s = Math.max(0, scroll - CURTAIN_START)`, `CURTAIN_START = 0`.

## Слои (параллакс)
- Текст: `translateY(-s * 0.03 * innerHeight px)`.
- Шторка: `translateY((1.2 - s * 0.12) * innerHeight px)` — шторка ×4 быстрее текста.
- Шторка не зажимается сверху (`cp = p`, без `Math.min`) — сплошной поток.

## Инерция колеса
- Накопление: `scrollV += e.deltaY * 0.028` (чувствительность за тик).
- Отсечение в обработчике: `scrollV *= 0.88`.
- Затухание в frame: `scrollV *= Math.exp(-dt * 3.5)`.
- Одобрено как «супер»; коэффициент `0.028` — фикс от 09-2026 (было `0.045`, «большое расстояние за тик»).

## Заливка
- Механика та же (привязана к `s`), со сдвигом и своим масштабом:
  `fillProg = clamp((s - 3) / 10, 0, 1)` — стартует раньше (было `s-5`).
- Построчная, на span-словах `.w`, квантование `((x*100)|0)` + кэш `_pct`.

## WebGL-лента
- `sceneLive`/рендер выключается после разворота:
  `sceneFade = clamp((UNFOLD_DIST - scroll) / 3)`, opacity канваса сбрасывается плавно.

## Правки (журнал)
- 2026-09: `0.045 → 0.028` (меньше расстояние за тик колеса).
- 2026-09: заливка `s-5 → s-3` (пускать раньше).
- 2026-09: фон `html,body` `#fff→` затемняется в чёрный одновременно с фейдом текста (`fadeIn=(s-5)/3`).
- 2026-09: новый экран 3 — афиша-стопка (4 постера Event2/event) в `#stack` (fixed, z-31). Сборка ~`s=STACK_P0-3`, листание скроллом `POSTER_DIST=2`/флип, `STACK_P0=14`. Режим работы — «черновая», будем править.
